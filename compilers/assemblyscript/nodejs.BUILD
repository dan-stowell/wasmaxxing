# Prebuilt Node.js runtime (linux/x64). Used only to drive the AssemblyScript
# compiler (`asc`), which is distributed as JavaScript.
package(default_visibility = ["//visibility:public"])

exports_files(["bin/node"])

filegroup(
    name = "node",
    srcs = ["bin/node"],
)
